# Spec-Kit: Analyze

Cross-artifact consistency report for spec.md, plan.md, and tasks.md (read-only, after /speckit.tasks).

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.analyze.agent.md`. Treat the user's message as `$ARGUMENTS`.
