# Spec-Kit: Tasks

Generate an actionable, dependency-ordered tasks.md from the plan and spec.

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.tasks.agent.md`. Treat the user's message as `$ARGUMENTS`.
