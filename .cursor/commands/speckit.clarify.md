# Spec-Kit: Clarify

Ask targeted clarification questions and encode answers into the spec (run before /speckit.plan).

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.clarify.agent.md`. Treat the user's message as `$ARGUMENTS`.
