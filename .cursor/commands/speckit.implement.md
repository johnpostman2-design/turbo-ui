# Spec-Kit: Implement

Execute the implementation by processing tasks from tasks.md.

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.implement.agent.md`. Treat the user's message as `$ARGUMENTS`.
