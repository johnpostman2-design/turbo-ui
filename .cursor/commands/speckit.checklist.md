# Spec-Kit: Checklist

Generate quality checklists to validate requirements (completeness, clarity, consistency).

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.checklist.agent.md`. Treat the user's message as `$ARGUMENTS`.
