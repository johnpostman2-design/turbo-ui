# Spec-Kit: Plan

Generate the technical implementation plan from the feature spec.

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.plan.agent.md`. Treat the user's message after the command as `$ARGUMENTS`.
