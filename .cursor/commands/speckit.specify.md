# Spec-Kit: Specify

Create or update the feature specification from a natural language description.

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.specify.agent.md`. The text the user typed after the command is the feature description (treat as `$ARGUMENTS`).
