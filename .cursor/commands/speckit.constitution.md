# Spec-Kit: Constitution

Establish or update the project constitution (principles and governance).

**Instructions:** Read and follow the full workflow in `.github/agents/speckit.constitution.agent.md`. Treat everything the user wrote after the command as the input (replacing `$ARGUMENTS` in the agent instructions).
